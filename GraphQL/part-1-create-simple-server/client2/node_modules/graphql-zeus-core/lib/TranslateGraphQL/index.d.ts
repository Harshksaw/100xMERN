import { Environment } from "../Models";
export interface TranslateOptions {
    schema: string;
    env?: Environment;
    host?: string;
    esModule?: boolean;
    subscriptions?: 'legacy' | 'graphql-ws';
}
export declare class TranslateGraphQL {
    static typescript: ({ schema, env, host, subscriptions }: TranslateOptions) => string;
    static typescriptSplit: ({ schema, env, host, esModule, subscriptions, }: TranslateOptions) => {
        const: string;
        index: string;
    };
}

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveOperations = void 0;
const graphql_js_tree_1 = require("graphql-js-tree");
const resolveOperations = (tree) => {
    const schemaNode = tree.nodes.find((n) => n.data.type === graphql_js_tree_1.TypeSystemDefinition.SchemaDefinition);
    if (!schemaNode)
        throw new Error('No operations in schema');
    const opsStrings = schemaNode.args.map((a) => `${a.name}: "${(0, graphql_js_tree_1.getTypeName)(a.type.fieldType)}" as const`);
    const opsString = `export const Ops = {
${opsStrings.join(',\n\t')}
}`;
    return opsString;
};
exports.resolveOperations = resolveOperations;
//# sourceMappingURL=index.js.map